AWSTemplateFormatVersion: '2010-09-09'
Description: Data Horizon — PipelineAudit DynamoDB table (mirrors terraform/modules/dynamodb).

Resources:
  PipelineAuditTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      TableName: PipelineAudit
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - { AttributeName: PK,     AttributeType: S }
        - { AttributeName: SK,     AttributeType: S }
        - { AttributeName: GSI1PK, AttributeType: S }
        - { AttributeName: GSI1SK, AttributeType: S }
      KeySchema:
        - { AttributeName: PK, KeyType: HASH }
        - { AttributeName: SK, KeyType: RANGE }
      GlobalSecondaryIndexes:
        - IndexName: GSI1_RunByPipeline
          KeySchema:
            - { AttributeName: GSI1PK, KeyType: HASH }
            - { AttributeName: GSI1SK, KeyType: RANGE }
          Projection: { ProjectionType: ALL }
      Tags:
        - { Key: Name, Value: PipelineAudit }

Outputs:
  TableName:
    Value: !Ref PipelineAuditTable
  TableArn:
    Value: !GetAtt PipelineAuditTable.Arn
